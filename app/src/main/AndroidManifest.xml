<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.google.gp_11">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AdminActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".AdminAddUserActivity" />
        <activity android:name=".AdminAddPlaceActivity" />
        <activity android:name=".AdminAddPackageActivity" />
        <activity android:name=".AdminAddGuideActivity" />
        <activity android:name=".AdminUpdateUser" />
        <activity android:name=".AdminUpdatePlace"></activity>
    </application>

</manifest>